@{foreach (var comment in ViewBag.comments)
    {
        <div>
            @comment.TimeCreated
            @comment.user
            <br>
            @comment.comment
        </div>
        <br>
    }
}